#include "main.h"

/**
 * more_numbers - prints numbers between 0 to 14
 * 10 times.
 * Return: no return.
 */
void more_numbers(void)
{
	int t, i;

	for (t = 48; t <= 57; t++)
	{
		for (i = 48; i <= 62; i++)
		{
			if (1 > 0)
			_putchar(i);
		}
	}
	_putchar('\n');
}
