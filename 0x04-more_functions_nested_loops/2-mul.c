#include "main.h"

/**
 * mul - multiply two integers
 *
 * @a: first parameter
 * @b: second parameter
 *
 * Return: multiplication of two numbers
 */
int mul(int a, int b)
{
	int m;

	m = 0;
	m = a * b;

	return (m);
}
