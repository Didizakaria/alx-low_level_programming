#include "main.h"
/**
 * _isalpha: checks for alphabetic character
 *
 * @c: character in ASCII
 *
 * Return: 1 if lowercase 0 if someting else
 *
 */
int _isalpha(int c)
{
	
	if ((i >= 65 && i<=90) || (i >= 97 && i<= 122))
	{
		return (1);
	}
	else
	{
		return (0);
	}
	_putchar('\n');
}

